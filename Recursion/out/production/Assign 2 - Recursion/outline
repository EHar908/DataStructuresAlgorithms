Important Note: You may not add data members to the Tree class to help solve these problems.  Instead, use return values
and/or parameters to the recursive methods to solve the problems.

Output:

Tree name
The keys (or values) of a binary tree, with the parent of the node in square brakcets to the right of the node.
    In the case of the root, report "no parent" as the parent node
    If there are no nodes in the tree, report "Empty tree"